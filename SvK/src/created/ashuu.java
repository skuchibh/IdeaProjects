package created;

import utils.InputReader;
import utils.OutputWriter;

public class ashuu {
    public void solve(int testNumber, InputReader in, OutputWriter out) {
/*
        int n = in.nextInt();
        int[] a = new int[n];
        for (int i = 0; i < n; i++) {
            a[i] = in.nextInt();
        }
        swg st = new swg(a);

        int q = in.nextInt();
        while (q-- > 0) {
            int op = in.nextInt();
            if (op == 0) {
                int at = in.nextInt() - 1;
                int value = in.nextInt();
                st.update(1, 0, a.length - 1, at, at, value);
            } else if (op == 1) {
                int from = in.nextInt() - 1;
                int to = in.nextInt() - 1;
                int even = 0;
                //out.println(st.get());
                even=st.query(1,0,a.length-1,from,to);
                out.println(even);
            } else if (op == 2) {
                int from = in.nextInt() - 1;
                int to = in.nextInt() - 1;
                int even = 0;
                even=st.query(1,0,a.length-1,from,to);
                out.println((to-from+1)-even);
            }
        }
*/
    }
}
