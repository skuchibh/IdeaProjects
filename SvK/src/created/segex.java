package created;

import utils.InputReader;
import utils.OutputWriter;

public class segex {
    public void solve(int testNumber, InputReader in, OutputWriter out) {
/*        swg st=new swg(a);
        out.println(st.query(0,7));
        st.update(1,0,7,2,7,100);
        out.println(st.query(0,7));
        out.println(st.get());*/
        int[] a = {1, 2, 3, 4, 5, 6,7,8};
    }
}
